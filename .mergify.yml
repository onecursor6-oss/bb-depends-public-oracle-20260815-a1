merge_protections_settings:
  reporting_method: check-runs
  post_comment: true

merge_protections:
  - name: BB dependency oracle harmless base gate
    if:
      - base = main
    success_conditions:
      - base = main
